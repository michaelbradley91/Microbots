<UserControl x:Class="Microbots.Views.RunMenuView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d">
    <Label HorizontalAlignment="Stretch" Background="LightGray" HorizontalContentAlignment="Right">
        <StackPanel  Orientation="Horizontal">
            <Button Name="StartMenuButton" IsEnabled="{Binding Path=IsStartButtonEnabled, Mode=TwoWay}" Height="45" FontSize="30" Padding="10,0" Margin="10,0" Click="StartMenuButtonClick">Start</Button>
            <Button Name="PauseMenuButton" IsEnabled="{Binding Path=IsPauseButtonEnabled, Mode=TwoWay}" Height="45" FontSize="30" Padding="10,0" Margin="10,0" Click="PauseMenuButtonClick">Pause</Button>
            <Button Name="StopMenuButton" IsEnabled="{Binding Path=IsStopButtonEnabled, Mode=TwoWay}" Height="45" FontSize="30" Padding="10,0" Margin="10,0" Click="StopMenuButtonClick">Stop</Button>
        </StackPanel>
    </Label>
</UserControl>
